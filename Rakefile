require 'rake'
require_relative 'app/server'

set :public_folder, 'public'

task :start do 
    puts "\n"
    puts ' 🎩 Sinatra server running on port 4567 ...'
    puts "\n"
    Sinatra::Application.run!
end